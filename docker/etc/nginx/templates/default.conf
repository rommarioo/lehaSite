

    server_tokens off;
    charset utf-8;

    server {
        listen 80 default_server;

        server_name _;

        location  /.well-known/ {
            root /var/www/certbot;
        }

        return 301 https://$host$request_uri;
    }





